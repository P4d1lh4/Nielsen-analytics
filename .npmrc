# @anthropic-ai/claude-agent-sdk declares a peer dependency on zod@^4, while this
# project pins zod@^3 (3.25+, which already ships the zod/v4 subpaths the SDKs use).
# legacy-peer-deps lets `npm install` resolve cleanly despite that peer mismatch.
legacy-peer-deps=true
